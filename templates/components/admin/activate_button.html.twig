{% set small = small ?? false %}
{% set use_label = use_label ?? false %}
{% set _title = ('label.'~ (recordActive ? 'deactivate' : 'activate')) | trans %}
{% set color = color ?? 'link ' %}

{% if use_label %}
    {% set label = _title %}
{% else %}
    {% set title = _title  %}
{% endif %}

{% include "components/button.html.twig" with {url: url('admin_edit', {category: application.categoryId, app: application.appId, uuid: recordID, action: 'activate', value: not recordActive }), color:color ~ (small ? 'btn-sm' : ''), icon: recordActive ? "eye" : "eye-slash" } %}